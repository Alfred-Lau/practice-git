import path from "path";

import "./style.less";
const name = (name) => name;

const num2 = (arr="") => {
    setTimeout(() => {
        console.log("I am very happy that I am engaged");
    }, 1000);
};

export {name,num2};

